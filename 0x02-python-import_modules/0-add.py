#!/usr/bin/python3
from add_0 import add

if __name__ == "__main__":
    """
    This function does the some of two numbers
    """
    a = 1
    b = 2

    res = add(a, b)
    print(f"{a} + {b} = {res}")
